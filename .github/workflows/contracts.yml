name: Contracts CI

on:
  push:
    paths:
      - 'contracts/**'
      - 'crates/protocol/**'
      - 'python-packages/contracts-py/**'
      - '.github/workflows/contracts.yml'
  pull_request:

jobs:
  validate-schemas:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate JSON schemas
        run: |
          for schema in contracts/schemas/*.json; do
            echo "Validating $schema..."
            python -m json.tool "$schema" > /dev/null || { echo "FAIL: Invalid JSON in $schema"; exit 1; }
          done

  cross-language-hash:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: astral-sh/setup-uv@v2
      - run: uv sync
      - name: Cross-language hash verification
        run: |
          echo "Cross-language golden vector tests require contract implementation (Milestone 2)."
          exit 1

  golden-vectors:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: astral-sh/setup-uv@v2
      - run: uv sync
      - name: Golden vector tests
        run: |
          echo "Golden vector tests require cross-language hash equivalence (Milestone 2)."
          exit 1
