# Requirements Validator

Validates bidirectional traceability between the SRS and verification matrix.

See `scripts/validate_requirements.py` for the main validator.

## Usage

```bash
uv run python scripts/validate_requirements.py
```
